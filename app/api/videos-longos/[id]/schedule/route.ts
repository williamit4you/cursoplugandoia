import { NextRequest, NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { LONG_FORM_PROJECT_TYPE, enqueueLongFormYoutube, parseLongFormMetadata } from "@/lib/longFormMarketing";
export async function POST(req: NextRequest, ctx: { params: { id: string } }) { const project = await prisma.codeVideoProject.findFirst({ where: { id: ctx.params.id, projectType: LONG_FORM_PROJECT_TYPE } }); if (!project) return NextResponse.json({ error: "Nao encontrado" }, { status: 404 }); const body = await req.json().catch(() => ({})); const meta = { ...parseLongFormMetadata(project.metadataJson), finalApproved: body.approve === true ? true : parseLongFormMetadata(project.metadataJson).finalApproved, planningApproved: true }; const updated = await prisma.codeVideoProject.update({ where: { id: project.id }, data: { metadataJson: JSON.stringify(meta) } }); const social = await enqueueLongFormYoutube(updated); return NextResponse.json({ project: updated, social }); }
