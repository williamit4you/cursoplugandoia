import { prisma } from "./lib/prisma"; async function main() { const configs = await prisma.shopeePipelineConfig.findMany(); console.log("Configs userVoiceRefUrl:"); configs.forEach(c => console.log(c.userVoiceRefUrl)); const personas = await prisma.creatorPersona.findMany(); console.log("Personas voiceRefUrl:"); personas.forEach(p => console.log(p.voiceRefUrl)); } main().finally(() => prisma.$disconnect());
